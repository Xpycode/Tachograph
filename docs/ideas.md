# Ideas / Backlog

Things parked from v1 spec — not now, maybe later.

| Idea | Notes |
|------|-------|
| Key-hold duration column | Would need keyDown/keyUp pairing + state machine. Adds a 4th column. Re-evaluate after using v1. |
| Pause/resume hotkey | Global shortcut to start/stop capture without touching the mouse. Need a non-collidey combo. |
| Filter / search box | Filter visible rows by key, label, or time range. |
| Per-app capture filter | "Only capture while Safari is frontmost." Needs frontmost-app polling via NSWorkspace. |
| Heat-map / histogram view | Tab next to the table. |
| CSV export to file | Save dialog → write file. Currently clipboard-only. |
| Multi-session history | Opt-in: persist sessions to Application Support; sessions browser UI. Significant feature. |
| Menu bar mode | Optional `MenuBarExtra` companion for quick start/stop. |
| Sound on key press (toggle) | Click-clack typewriter feedback. Probably no, but it's an idea. |
| Auto-clear after N minutes | Privacy-friendly: discard rows older than X automatically while capturing. |
